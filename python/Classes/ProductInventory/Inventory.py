Inventory:
    product = []

    def addProduct(self, newProduct)
        self.product[newProduct.getId()] = newProduct

    def removeProduct(self, productName)
        self.product.del[productName]

    def printProductList(self)
        for thisProduct in set(product)
            print thisProduct.getId()": #"thisProduct.getQuantityOnHand()" $"thisProduct.getPrice()
